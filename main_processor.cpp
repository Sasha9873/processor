#include "header.h"
#include "processor_header.h"
#include "processor.cpp"


int main()
{
    FILE* code_txt = fopen("code.txt", "r");

    processor(Stack* stack1);

    fclose(code_txt);

    return 0;
}
